const sqlite3 = require("sqlite3").verbose();
const db = new sqlite3.Database("./database/daily.sqlite");

db.serialize(() => {

    db.run(`
        CREATE TABLE IF NOT EXISTS daily_rewards (
            userId TEXT PRIMARY KEY,
            lastClaim INTEGER
        )
    `);
});

module.exports = db;